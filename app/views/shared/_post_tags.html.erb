	  <% local_color = get_tag_color %> 	  
	  
	  <% post.tags_from_hash.each do |e| %>
        <div><h4 class="flex-grow px-1 text-xs m-1 py-1 font-medium title-font text-gray-700 <%= local_color %> hover:bg-blue-500 rounded-lg"><%= e %></h4></div>
	  <% end %>