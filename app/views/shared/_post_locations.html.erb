	  <% local_color = get_location_color %> 
	  <% post.locations_from_hash.each do |e| %>
        <div><h1 class="flex-grow px-1 text-xs m-1 py-1 font-medium title-font text-gray-700 <%= local_color %> rounded-lg hover:bg-cyan-500"><%= e %></h1></div>
	  <% end %>